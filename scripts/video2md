#!/usr/bin/env bash
set -euo pipefail

ENV_FILE="${VIDEO2MD_ENV_FILE:-$HOME/.video2md-cli.env}"
BIN="${VIDEO2MD_CLI_BIN:-$HOME/.video2md-cli/bin/mp4-md}"

if [[ -f "$ENV_FILE" ]]; then
  set -a
  set +u
  # shellcheck disable=SC1090
  source "$ENV_FILE"
  set -u
  set +a
fi

exec "$BIN" "$@"
